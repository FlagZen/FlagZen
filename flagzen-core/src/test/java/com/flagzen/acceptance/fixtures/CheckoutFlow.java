package com.flagzen.acceptance.fixtures;

/**
 * Test fixture: feature interface for acceptance testing.
 * In production, this would be annotated with @Feature("checkout-flow").
 */
public interface CheckoutFlow {
    String execute();
}
